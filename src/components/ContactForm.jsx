import { motion } from "framer-motion";

const ContactForm = () => {
  return (
    <motion.form className="card-surface">
      <h1 className="text-center">Contact Form Later!</h1>
    </motion.form>
  );
};

export default ContactForm;
